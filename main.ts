radio.onReceivedValue(function (name, value) {
    if (name == "posx") {
    	
    }
})
basic.clearScreen()
radio.setGroup(1)
let yo = game.createSprite(2, 2)
let otro = game.createSprite(4, 4)
let tiempo = 500
basic.forever(function () {
	
})
